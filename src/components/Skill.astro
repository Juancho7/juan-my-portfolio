---
const { skillName, logo, colors } = Astro.props;
---

<div
  class={`p-2 flex max-[500px]:justify-center items-center gap-x-2 font-medium ${colors} rounded`}
>
  {skillName}
  <img src={logo} alt={skillName} class='w-8' />
</div>
