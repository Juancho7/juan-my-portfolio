---
const { skillName, logo, colors } = Astro.props;
---

<div
  class={`h-full p-2 flex max-[500px]:justify-center items-center gap-x-2 font-medium ${colors} rounded`}
>
  <span>{skillName}</span>
  <img src={logo} alt={skillName} class='w-8' />
</div>
